<!DOCTYPE html>
<html lang="end">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>New Message</title>
        <link rel="stylesheet" href="/styles.css">
    </head>
    <body>
        <main>
        <h2><%= title %></h2>
        <a href="/">
            <button class="backBtn" type="button">Back</button>
        </a>
        <form method="POST" action="/new">
            <div class="input-group">
                <label for="name">Name</label>
                <input id="name" name="messageUsername" type="text" placeholder="Alex">
            </div>
            <div class="input-group">
                <label for="messageText">Message</label>
                <textarea name="messageText" id="messageText" placeholder="What do you want to post?"></textarea>
            </div>
            <div class="button-group">
                <button class="postBtn" type="submit">Post</button>
            </div>
        </form>
        </main>
    </body>
</html>
